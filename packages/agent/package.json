{
  "name": "@workspace-os/agent",
  "private": true,
  "version": "0.0.0",
  "description": "Agent loop, personas, model gateway, MCP/tool client, sandbox runner",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "echo \"no tests yet\""
  },
  "dependencies": {
    "@workspace-os/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
